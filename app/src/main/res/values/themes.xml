<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AriithmeticProblemGeneratorAndVerifier" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AriithmeticProblemGeneratorAndVerifier" parent="Base.Theme.AriithmeticProblemGeneratorAndVerifier" >
        <item name="android:textSize">25sp</item>
    </style>
    <style name="ProblemOutput">
        <item name="android:textSize">45sp</item>
        <item name="android:gravity">center</item>
    </style>
</resources>